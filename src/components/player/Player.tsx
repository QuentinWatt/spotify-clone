import React from "react";

const Player: React.FC = () => {
  return (
    <div className="border-t border-dark-800 bg-dark-800 w-full h-28 flex items-center justify-center absolute bottom-0">
      <div>Play Icon</div>
    </div>
  );
};

export default Player;
